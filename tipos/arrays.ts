(() => {
  const numbers: number[] = [1, 2, 3, 4, 5, 7, 8, 9, 10];

  const villians = ["Freezer", "Cell", "Bills", "Goku Black"];

  villians.forEach((v) => console.log(v.toUpperCase()));

  numbers.forEach((n) => console.log(n));
})();
